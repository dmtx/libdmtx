if ENABLE_PHP
   PHP_DIR = php
endif

if ENABLE_PYTHON
   PYTHON_DIR = python
endif

if ENABLE_RUBY
   RUBY_DIR = ruby
endif

if ENABLE_VALA
   VALA_DIR = vala
endif

SUBDIRS = $(PHP_DIR) $(PYTHON_DIR) $(RUBY_DIR) $(VALA_DIR)
